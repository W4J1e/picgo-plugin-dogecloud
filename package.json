{
  "name": "picgo-plugin-dogecloud",
  "version": "0.0.1",
  "description": "picgo upload plugin for dogecloud",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/w4j1e/picgo-plugin-dogecloud",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "dev": "tsc -w -p .",
    "pub": "rm -rf dist/* && npm build && npm publish",
    "patch": "npm version patch -m 'bump version into v%s' && git push origin main && git push origin --tags",
    "minor": "npm version minor -m 'bump version into v%s' && git push origin main && git push origin --tags",
    "major": "npm version major -m 'bump version into v%s' && git push origin main && git push origin --tags"
  },
  "keywords": [
    "dogecloud",
    "s3",
    "picgo",
    "picgo-gui-plugin",
    "picgo-plugin"
  ],
  "author": "W4J1e",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.10.1",
    "eslint": "^5.0.1",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0",
    "picgo": "^1.4.0 >1.4.16",
    "tslint": "^5.10.0",
    "tslint-config-standard": "^7.1.0",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "aws-sdk": "^2.839.0",
    "file-type": "^16.2.0",
    "mime": "^2.5.2"
  }
}
